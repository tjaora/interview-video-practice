//video link: https://youtu.be/_na7OIqF9OY?si=ZwRTK_n6sXM3RCLy

//console.log(0.1 + 0.2 === 0.3);
//console.log(0.1 + 0.2);

//const obj = { a: 1, b: 2 };
//console.log(Object.keys(obj).length);

//const obj2 = { c: 3, d: 4 };
//shallow copy
//const copyObj = { ...obj, ...obj2 };
//console.log(copyObj);

//reduce method
//const arr = [1, 2, 3, 4, 5];
//const sum = arr.reduce((acc, curr) => acc + curr, 0);
//console.log(sum);

//math methods
//console.log(Math.max(...arr));
//console.log(Math.min(...arr));
//const result = arr.map((num) => num > 2);
//console.log(result);

//filter method
//const filtered = arr.filter((num) => num > 2);
//console.log(filtered);

//different between math methods and filter method
//console.log(Math.max(...filtered));   
//console.log(Math.min(...filtered));

//use effect hook vs use layout effect hook ?
//use effect hook runs after render and paint
//use layout effect hook runs after render but before paint
//use memo hook vs use callback hook ?
//use memo hook memoizes the result of a function
//use callback hook memoizes the function itself
//what is memoization ?
//memoization is an optimization technique used to speed up function calls by caching the results of expensive function calls and returning the cached result when the same inputs occur again.
//radix vs context api ?
//radix is a number system base
//context api is a way to pass data through the component tree without having to pass props down manually at every level
//which is better for state management redux or context api ?
//it depends on the complexity of the application. redux is better for large applications with complex state management needs. context api is better for small to medium applications with simple state management needs
//difference between package.json and package-lock.json ?
//package.json is a file that contains metadata about the project and its dependencies
//package-lock.json is a file that contains the exact versions of the dependencies installed in the project
//type of list in html ?
//ordered list <ol>
//unordered list <ul>
//description list <dl> 
//difference between inline and block elements?
//inline elements do not start on a new line and only take up as much width as necessary
//block elements start on a new line and take up the full width available
//semantic html vs non-semantic html?
//semantic html uses elements that convey meaning about the content they contain
//non-semantic html uses elements that do not convey meaning about the content they contain
//difference between dev tag and span tag?
//div tag is a block-level element used to group other elements
//span tag is an inline element used to group text or other inline elements
//difference between meter and progress tags?
//meter tag represents a scalar measurement within a known range
//progress tag represents the completion progress of a task
//diference between css border and outline?
//border is part of the element's box model and takes up space
//outline is drawn outside the element's border and does not take up space
//css position property values?
//static, relative, absolute, fixed, sticky 
//how to overlap elements using css?
//using position property with z-index 
